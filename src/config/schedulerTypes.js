const SchedulerTypes = Object.freeze({
    WALLET_GENERATION: 'WalletGeneration',
    TOKEN_DISTRIBUTION: 'TokenDistribution',
    // Add more types as needed in the future
  });
  
  module.exports = SchedulerTypes;
  